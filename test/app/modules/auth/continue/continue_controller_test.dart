import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:commandaapp/app/modules/auth/continue/continue_controller.dart';
import 'package:commandaapp/app/modules/auth/continue/continue_module.dart';

void main() {
  initModule(ContinueModule());
  // ContinueController continue;
  //
  setUp(() {
    //     continue = ContinueModule.to.get<ContinueController>();
  });

  group('ContinueController Test', () {
    //   test("First Test", () {
    //     expect(continue, isInstanceOf<ContinueController>());
    //   });

    //   test("Set Value", () {
    //     expect(continue.value, equals(0));
    //     continue.increment();
    //     expect(continue.value, equals(1));
    //   });
  });
}
