import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:commandaapp/app/modules/auth/continue/continue_page.dart';

void main() {
  testWidgets('ContinuePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ContinuePage(title: 'Continue')));
    //  final titleFinder = find.text('Continue');
    //  expect(titleFinder, findsOneWidget);
  });
}
