import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:commandaapp/app/modules/home/meus_pedidos/meus_pedidos_page.dart';

void main() {
  testWidgets('MeusPedidosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MeusPedidosPage(title: 'MeusPedidos')));
    //  final titleFinder = find.text('MeusPedidos');
    //  expect(titleFinder, findsOneWidget);
  });
}
