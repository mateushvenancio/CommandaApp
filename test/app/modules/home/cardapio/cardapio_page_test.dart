import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:commandaapp/app/modules/home/cardapio/cardapio_page.dart';

void main() {
  testWidgets('CardapioPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CardapioPage(title: 'Cardapio')));
    //  final titleFinder = find.text('Cardapio');
    //  expect(titleFinder, findsOneWidget);
  });
}
