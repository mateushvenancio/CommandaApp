import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:commandaapp/app/modules/home/cardapio/cardapio_controller.dart';
import 'package:commandaapp/app/modules/home/cardapio/cardapio_module.dart';

void main() {
  initModule(CardapioModule());
  // CardapioController cardapio;
  //
  setUp(() {
    //     cardapio = CardapioModule.to.get<CardapioController>();
  });

  group('CardapioController Test', () {
    //   test("First Test", () {
    //     expect(cardapio, isInstanceOf<CardapioController>());
    //   });

    //   test("Set Value", () {
    //     expect(cardapio.value, equals(0));
    //     cardapio.increment();
    //     expect(cardapio.value, equals(1));
    //   });
  });
}
