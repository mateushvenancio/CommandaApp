import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:commandaapp/app/modules/home/cardapio/checkout_item/checkout_item_page.dart';

void main() {
  testWidgets('CheckoutItemPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CheckoutItemPage(title: 'CheckoutItem')));
    //  final titleFinder = find.text('CheckoutItem');
    //  expect(titleFinder, findsOneWidget);
  });
}
