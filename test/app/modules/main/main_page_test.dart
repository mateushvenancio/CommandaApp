import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:commandaapp/app/modules/main/main_page.dart';

main() {
  testWidgets('MainPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MainPage(title: 'Main')));
    //  final titleFinder = find.text('Main');
    //  expect(titleFinder, findsOneWidget);
  });
}
