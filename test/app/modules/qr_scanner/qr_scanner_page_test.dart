import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:commandaapp/app/modules/qr_scanner/qr_scanner_page.dart';

main() {
  testWidgets('QrScannerPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(QrScannerPage(title: 'QrScanner')));
    //  final titleFinder = find.text('QrScanner');
    //  expect(titleFinder, findsOneWidget);
  });
}
