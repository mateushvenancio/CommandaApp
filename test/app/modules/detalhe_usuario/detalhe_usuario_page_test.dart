import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:commandaapp/app/modules/detalhe_usuario/detalhe_usuario_page.dart';

void main() {
  testWidgets('DetalheUsuarioPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DetalheUsuarioPage(title: 'DetalheUsuario')));
    //  final titleFinder = find.text('DetalheUsuario');
    //  expect(titleFinder, findsOneWidget);
  });
}
